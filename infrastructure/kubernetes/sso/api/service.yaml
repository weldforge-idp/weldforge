apiVersion: v1
kind: Service
metadata:
  name: sso-api
  namespace: sso
  labels:
    app: sso-api
spec:
  selector:
    app: sso-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
