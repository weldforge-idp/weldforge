---
name: session-2026-09-10-sprint5-gaps-sprint6
description: Sprint 5 gaps closed and Sprint 6 built on two local, unpushed, undeployed branches; one live prod bug fixed on the branch; max_age/prompt=login deviations open
metadata:
  node_type: memory
  type: project
---

Snapshot **2026-09-10**. Re-verify with `git branch -a` and `gh pr list` before acting.

- `fix/conf-sprint-5-gaps` (from `main` @ `d79b62f`) and `feat/conf-sprint-6`
  (stacked on it) are **committed locally, not pushed, not deployed** — the user
  chose "commit locally, stop". Migrations reach **V56**.
- Deploys go through Flux: bump `infrastructure/apps/weldforge/overlays/{staging,production}`.
- Live prod bug fixed only on the Sprint 6 branch: `/api/auth/tenants/verify-contact-page`
  answers `400 "Conversion = ';'"` until deployed.
- Open, needs a product decision: OIDC `max_age` enforced as MFA-factor
  freshness (browser `400 mfa_required`); `prompt=login` ignored. Conformance
  statement D1/D2.
- Decisions taken: NIST 800-63B password defaults + HIBP screening (fail-open);
  pre-existing SAML SPs pinned to `PasswordProtectedTransport` (V56).
- Records: `docs/product/standards-conformance-backlog.md` §7,
  `docs/security/hardening-backlog.md` F44–F53,
  `docs/compliance/standards-conformance.md`, `docs/adr/`.
