---
name: session-2026-09-10-sprint5-gaps-sprint6
description: Sprint 5 gaps + Sprint 6 merged (#93, #94) and live as sha-797c70b; how to deploy via Flux from the node; max_age/prompt=login and B-API-2 open
metadata:
  node_type: memory
  type: project
---

Snapshot **2026-09-10**. Re-verify before acting.

- PRs #93 and #94 merged; `sha-797c70b` live on staging and production
  (infrastructure `c145ce3`, `ad7b8f7`). Migrations at **V56**.
- Deploy: bump `infrastructure/apps/weldforge/overlays/{staging,production}`,
  then `ssh tech01`, `export KUBECONFIG=/etc/rancher/k3s/k3s.yaml`,
  `flux reconcile source git flux-system -n flux-system` and
  `flux reconcile kustomization weldforge-<env> -n flux-system`.
  Pre-deploy dump: `kubectl -n postgres create job <name> --from=cronjob/postgres-backup`.
- Staging has no `leap` tenant; smoke-test it with `default`.
- Traefik's security-headers middleware must not set `referrerPolicy` — it
  overwrote the app's `no-referrer` (removed in infrastructure `0e52fa0`).
- Open, needs a product decision: OIDC `max_age` enforced as MFA-factor
  freshness (browser `400 mfa_required`); `prompt=login` ignored (D1/D2).
- Open: B-API-2 — no Bean Validation provider, so `@Valid` is enforced nowhere.
- Records: `docs/product/standards-conformance-backlog.md` §7,
  `docs/security/hardening-backlog.md` F44–F53, `docs/compliance/standards-conformance.md`.
