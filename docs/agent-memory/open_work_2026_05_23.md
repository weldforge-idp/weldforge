---
name: open-work-snapshot-2026-05-23
description: Point-in-time snapshot of what was open + production state at the end of the 2026-05-20 → 2026-05-23 session; for resumption on another machine
metadata:
  node_type: memory
  type: project
  originSessionId: 2026-05-20-per-tenant-auth-urls
---

Snapshot taken **2026-05-23** at the end of the per-tenant-auth-URL +
security-hardening + portability session. Project memory decays — re-verify
each fact below with the listed command before acting on it.

## Production state

- **Per-tenant subdomain auth URLs** (the `*.sso.weldforge.org` shape from
  `docs/auth-url-spec.md`): **shipped to code but unreachable in prod**.
  `host demo.sso.weldforge.org` returns NXDOMAIN; apex TLS cert SAN is
  `DNS:sso.weldforge.org` only. No regression — apex `sso.weldforge.org`
  still serves login (resolving to the `default` tenant via fallback). The
  user-visible loss is per-tenant branding on bookmarked legacy URLs.
  **Verify:** `host demo.sso.weldforge.org` and
  `echo | openssl s_client -connect sso.weldforge.org:443 -servername sso.weldforge.org 2>/dev/null | openssl x509 -noout -ext subjectAltName`.
- **JWT tenant-binding, iss claim, slug holdback, 415 Content-Type guard,
  noindex, tenant verify/unverify endpoints, V2a email-based verification
  challenge**: all live in production (deploys auto-fire on push to main).
  **Verify**: `curl -sS -X POST -H 'Content-Type: application/x-www-form-urlencoded' -d 'a=b' https://sso.weldforge.org/api/auth/login` should return **HTTP 415**.

## Open agenda

1. **Wildcard DNS + TLS** — `*.sso.weldforge.org` A-record + Google
   Certificate Manager wildcard cert. **Pending user** (needs `gcloud` creds
   + DNS Admin on `weldforge.org`). Operator playbook is at
   `docs/runbooks/wildcard-tls-setup.md`. Until this is done, the
   per-tenant subdomain feature stays unreachable.
2. **TechMetropolis + WriteBuddy heads-up** — external partners weren't
   notified of the URL-contract change (legacy `?tenant=` removed; new
   shape is `https://{slug}.sso.weldforge.org/…`). Drafts existed in
   `/tmp/` during the original session but never committed (point-in-time
   ephemera); regenerate from `docs/auth-url-spec.md` as needed. **Held
   intentionally** until subdomain URLs actually resolve — sending the
   notice now would point partners at NXDOMAIN URLs.
3. **Identity-proofing V2b** — domain gate: refuse to auto-issue
   email-verification when `contact_email`'s domain doesn't match the
   tenant's OIDC `webOrigins`. Designed in
   `docs/auth-url-spec.md` §"V2 roadmap", not built.
4. **Identity-proofing V2c** — watchword auto-flag: slugs containing
   `bank`, `pay`, `secure`, `official` queue for human review instead of
   going live. Designed in spec, not built.
5. **Identity-proofing V2d** — positive verified-tenant logo badge in the
   auth-shell (counterpart of the V1 unverified warning). Designed in
   spec, not built.
6. **Prometheus alert on `sso.mail.send` failure counter** — orthogonal
   observability gap flagged in #38 SendGrid PR. `SmtpMailService` logs
   delivery failures at WARN but the triggering security operation still
   returns success, so any silent SMTP break (not just plan-related)
   leaves the platform without a user-visible signal.
7. **SendGrid smoke test on 2026-07-14** — 48h before trial perks expire
   (2026-07-16). Cheap delivery verification:
   `curl -X POST https://sso.weldforge.org/api/auth/forgot-password -H 'Content-Type: application/json' -d '{"identifier":"<test-email>"}'`
   then check SendGrid Activity Feed for "Delivered". The user clarified in
   #38 that the account is already on the Free tier — see
   [[sendgrid-trial-deadline]] — but the smoke test is cheap insurance.
8. **`origin/dev` sync** — 61 commits behind `origin/main` as of 2026-05-23
   and growing. See [[origin-dev-branch-behind-main]].

## Things explicitly NOT to do on resume

- **Don't re-send the TechMetropolis / WriteBuddy heads-up docs from any
  cached form.** If they need to go out, regenerate from the live
  `docs/auth-url-spec.md` — the spec has evolved since the original drafts
  (V37 slug-holdback, V38 identity-proofing V1, V39 verification tokens
  all landed after the drafts were written).
- **Don't run the wildcard-TLS runbook on the user's behalf.** It needs
  their `gcloud` creds and DNS Admin on the `weldforge.org` zone; I can't
  authenticate as them for that scope. Walk them through step-by-step if
  asked.
- **Don't `git push origin main:dev` without explicit user confirmation.**
  The `dev` branch may be intentionally pinned for an external observer
  or release marker. Always ask.

## What's covered in repo-committed docs (no need for memory entries)

- The full security arc (#32–#37): `docs/auth-url-spec.md`
- DNS+TLS setup: `docs/runbooks/wildcard-tls-setup.md`
- Tenant identity-proofing V1 + V2a surface: `docs/auth-url-spec.md`
  §"Tenant identity-proofing"

When these docs change, the source of truth is the doc — not the memory
note. Memory only captures what isn't documented elsewhere.
